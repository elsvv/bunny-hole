{
  "name": "bunny-hole",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "bash build.sh",
    "dev": "npx esbuild src/main.ts --bundle --outfile=dist/app.js --target=es2022 --watch --servedir=.",
    "check": "npx tsc --noEmit",
    "test": "bash -c 'mkdir -p dist/tests && for f in tests/*.test.ts; do npx esbuild \"$f\" --bundle --outfile=\"dist/tests/$(basename \"$f\" .ts).js\" --platform=node --format=esm --packages=external; done && node --test dist/tests/*.test.js'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "esbuild": "^0.27.4",
    "typescript": "^5.9.3"
  }
}
